import { SortBookByPipe } from './sort-book-by.pipe';

describe('SortBookByPipe', () => {
  it('create an instance', () => {
    const pipe = new SortBookByPipe();
    expect(pipe).toBeTruthy();
  });
});
